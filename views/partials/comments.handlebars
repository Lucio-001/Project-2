<div class="comments">
  {{#each comments as |comment|}}
    <section class="comment">
      <div class="meta">
        {{comment.user.username}}
      </div>
      <div class="text">
        {{comment.comment_text}}
      </div>
    </section>
  {{/each}}
</div>